import numpy as np


# view user keys in database
keys = np.load('user_keys.npy')
print(len(keys))
print(keys)

# view name keys in database
keys = np.load('name_keys.npy')
print(len(keys))
print(keys)